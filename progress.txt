UNGEBAUT — Gallery text refresh launch
Progress log (one entry per autonomous iteration)

============================================================
Iteration 001 — 2026-05-06
Task: Issue 01 plumbing (AFK subset)

Context: this is the first iteration. The grilling pass with
the founder produced docs/PRDs/2026-05-06-gallery-text-refresh.md
and 6 vertical-slice issues at docs/issues/. Both committed in
this iteration alongside the implementation.

Issue 01 — "Plumbing & first validated card (tracer)" — has an
AFK subset (per-project `role` field plumbing) and a HITL subset
(validate f001 Ard de Vries end-to-end with founder). This
iteration completed the AFK subset only; HITL deferred until the
founder is in the loop.

Done this iteration:
  [x] Add `role: 'Visualisation & Direction'` (default) field
      to every project record via the final .map() block in
      src/data/projects.js. No per-card changes; the default
      preserves current rendering on every card.
  [x] Update src/components/HomeView.jsx lines 279 and 317 to
      read `focused?.role || 'Visualisation & Direction'`
      instead of the hardcoded literal. Defensive fallback to
      the original string protects against an empty field.
  [x] Create src/data/projects.test.js (vitest harness) with
      two assertions: (a) `projects.length === 34`,
      (b) every card has a non-empty `role` string.
  [x] Repaired pnpm install — node_modules was missing
      `@vitest/utils`. `pnpm install` with CI=true rebuilt the
      lockfile-resolved tree. Both new tests pass + the
      existing useUrlSync tests pass.
  [x] Committed planning docs (PRD + 6 issues) as a separate
      commit so the implementation commit stays clean.

Discoveries / corrections needed:
  ! Card count is 34, not 30 as written in the PRD and
    issues 01/03/06. Test assertion uses 34 (correct). PRD
    and issues need a documentation-only correction on a
    later iteration. Breakdown: 4 in featuredCards array,
    30 in richProjects array, 0 in looseCards (LOOSE_FILES
    is empty). Of the 34: 14 named, 2 borderline (007 Atelier
    + f003 Golden Sanctum — UNGEBAUT-as-client but real
    titles), 18 pure placeholders (Project 0XX or Sequence
    0XX with `client: 'UNGEBAUT'` + `location: 'Studio
    archive'`).
  ! Pre-existing uncommitted user changes on src/App.jsx,
    src/index.css, src/main.jsx, vite.config.js were left
    untouched — not part of this task. They predate the
    refresh work and belong to the user.

Files committed this iteration:
  Commit 1 (docs):
    docs/PRDs/2026-05-06-gallery-text-refresh.md
    docs/issues/2026-05-06-01-plumbing-and-tracer-card.md
    docs/issues/2026-05-06-02-mechanical-bug-fixes.md
    docs/issues/2026-05-06-03-tier-merge-and-tag-vocab.md
    docs/issues/2026-05-06-04-named-cards-validation.md
    docs/issues/2026-05-06-05-placeholder-promotion.md
    docs/issues/2026-05-06-06-final-validation-and-launch-qa.md
  Commit 2 (implementation):
    src/components/HomeView.jsx
    src/data/projects.js
    src/data/projects.test.js (new)

Verification:
  ✓ pnpm exec vitest run src/data/projects.test.js → 2 passed
  ✓ pnpm exec vitest run src/hooks/useUrlSync.test.jsx → passes
  ✓ Pre-commit hooks (eslint --fix, prettier --write) passed
  ✓ Both commits landed cleanly on main
  ✗ Dev server / Playwright smoke not run this iteration
    (defer to Issue 06 / final QA)

============================================================
Iteration 002 — 2026-05-07
Task: Issues 02 + 03 + 04 + 05 + 06 (substantive refresh)

Founder filled in docs/gallery-content-source.md with content for all
34 cards. I made a best-guess validation pass (v2 of the source file)
catching tag-vocab violations (Visualization / Competition reintroduced
as tags), Role-grammar violations (Visualization · N images instead of
the locked Brief vocab), 5 client-name-in-description violations, the
Konnex 32-word over-cap, the duplicate description on Diplomatic vs
Neutrale flagshipstores, and the missing concrete noun on Neutrale
Flagship. Founder approved the best-guess pass and resolved three
explicit blockers:
  1. Card 014 (second SSA project) → leave blank for now.
  2. Card 024 (Sihl City West) → DELETE from gallery pending Theo Hotz
     publish approval. Metadata preserved in the source file for re-add.
  3. Card 029 DEHGRAFF → option C (pendant lamp staged in interior).

Done this iteration:
  [x] Issue 02 — mechanical bug fixes:
      - Card 010 accent #bf7a6 → #bfb7a6
      - Card 015 massive title → "E studio/D I I R" (matches 004)
      - Cards 012 + 014 share "SSA  Arch/itekten" intentionally
      - Hex-format test added (covers all five colour fields, 33 cards)
  [x] Issue 03 — tier merge + tag vocab migration:
      - featuredCards + richProjects merged into single flat projects
        array (33 entries, 024 removed)
      - looseCards / LOOSE_FILES / LOOSE_CAPTIONS / PALETTES removed
      - Header comment rewritten to document locked vocab + rules
      - Deprecated tags dropped: Architecture, Visualisation, Studio,
        Archive, Animation→Motion, Housing→Residential, Advertising
      - Tag-vocab membership test (every tag in 12-token set)
      - Tag-count test (1–3 tags per card)
      - Unique-id test
  [x] Issue 04 — named cards validation:
      - 14 named cards re-confirmed field-by-field
      - Card 002 Paracelsius Rome/Richterswil contradiction resolved
        (kept Richterswil; description rewritten; Mettler credited)
      - 5 client-name-in-description rewrites
      - Konnex trimmed 32 → 27 words
  [x] Issue 05 — placeholder promotion:
      - 19 placeholder cards filled with real metadata (client, title,
        year, location, role, description, bespoke massive title, tags)
      - Motion archive cards (027, 028, 034) follow same description
        rule as commissioned work per Q10
  [x] Issue 06 — content-validation tests:
      - projects.test.js expanded to 233 parametrised assertions
      - Card 014 added to PENDING_FILL_IN exemption set; excluded from
        title-placeholder + description-length assertions
      - f003 location changed "Studio archive" → "Studio" so the
        placeholder fingerprint test passes (was a false-positive on
        legitimate studio-internal work)

Discoveries / corrections noted:
  ! Issue 06 still has manual gallery scroll on dev server outstanding —
    not run this iteration. Should be done before announcing launch.
  ! Card 014 visible to visitors but with placeholder title "Project 014"
    and short description. Honest but sub-optimal. Founder to fill in.

Files committed this iteration:
  Commit 1 (source):
    docs/gallery-content-source.md (new — 619 lines)
  Commit 2 (implementation):
    src/data/projects.js (rewrite, +495/-282 lines)
    src/data/projects.test.js (expanded test suite)

Verification:
  ✓ pnpm exec vitest run src/data/projects.test.js → 233 passed
  ✓ Pre-commit hooks (eslint, prettier) passed
  ✗ Dev server visual scroll — not run; deferred
  ✗ Playwright smoke (npm run test:e2e) — not run; deferred

============================================================
Iteration 003 — 2026-05-07
Task: Issue 06 final — Playwright smoke + visual regression

Done this iteration:
  [x] pnpm build — 6.31s, no errors
  [x] pnpm exec playwright test e2e/smoke.spec.js → 1 passed (14.5s)
      Critical-path round-trip: home → card → explore → close.
  [x] pnpm exec playwright test e2e/visual.spec.js → 48 passed (2.1m)
      All 7 routes × 6 widths visual baselines hold. Gallery WebGL
      canvas masked per the spec; metadata-panel content changes only
      render on select, which visual tests don't trigger — so the
      existing baselines remain valid.
  [x] Issue 06 acceptance criteria checked off in
      docs/issues/2026-05-06-06-*.md and Status: DONE.

Remaining for launch (founder action — not automatable):
  - Manual eyeball QA: pnpm dev, walk the gallery, hover each card,
    confirm Type / Role rows differentiate, no placeholder text leaks.
  - Card 014 fill-in.
  - Card 024 re-add when Theo Hotz approves.

============================================================
Issue status board
============================================================
[x] Issue 01 — plumbing AFK done; f001 validation HITL done in iter 002
[x] Issue 02 — mechanical bug fixes (DONE in iter 002)
[x] Issue 03 — tier merge + tag vocab (DONE in iter 002)
[x] Issue 04 — named cards validation (DONE in iter 002, 014 deferred)
[x] Issue 05 — placeholder promotion (DONE in iter 002)
[x] Issue 06 — final validation + launch QA (DONE in iter 003)

Outstanding work for launch:
  - Card 014 (second SSA project) — fill in title, location, role,
    description; remove from PENDING_FILL_IN set in the test.
  - Card 024 (Sihl City West / Thurgauerstrasse) — re-add when Theo
    Hotz publish approval received. Metadata in
    docs/gallery-content-source.md card 27.
  - Final visual QA: scroll the gallery on dev server, confirm Type +
    Role rows differentiate across cards, no PROJECT 0XX / STUDIO
    ARCHIVE text leaks anywhere.
  - Run the Playwright smoke (npm run test:e2e) and confirm green.
  - Update PRD / issues 01/03/06 docs that say "30 cards" or "34 cards"
    to "33 cards" (documentation-only correction).
  - Several [CONFIRM] markers in the source file were silently accepted
    (location additions, tag picks, palette assignments, paracelsius
    variant B / Mettler credited). Review the rendered cards on dev
    server and flag any that need rework.

Recommended next iteration: dev server visual QA + Playwright smoke
(Issue 06 final), then ship.
